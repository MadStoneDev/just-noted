// Rendered by NoteWrapper (in the (app) layout) via the URL. Marker page.
// Access is enforced server-side in every admin action (assertAdmin); the
// admin view itself only renders for role >= 10.
export default function AdminPage() {
  return null;
}
