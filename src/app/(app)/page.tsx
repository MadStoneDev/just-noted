// The notes + editor experience is rendered by NoteWrapper in the (app) layout
// based on the URL. This page is a marker so "/" exists in the route group.
export default function Home() {
  return null;
}
